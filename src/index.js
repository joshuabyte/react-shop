import React from "react";
//Careful with the brackets {} below. If there's an error, remove them. Also, be sure that the string import matches with the resource trying to be imported.
import ReactDOM from "react-dom";
import App from "./routes/App.jsx";

ReactDOM.render(<App/>, document.getElementById("app"));
